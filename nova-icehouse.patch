diff --git a/nova/compute/manager.py b/nova/compute/manager.py
index 55075c5..42045a5 100644
--- a/nova/compute/manager.py
+++ b/nova/compute/manager.py
@@ -1174,6 +1174,7 @@ class ComputeManager(manager.Manager):
 
         return [_decode(f) for f in injected_files]
 
+    @hooks.add_hook('run_instance')
     def _run_instance(self, context, request_spec,
                       filter_properties, requested_networks, injected_files,
                       admin_password, is_first_time, node, instance,
@@ -2251,6 +2252,7 @@ class ComputeManager(manager.Manager):
     @reverts_task_state
     @wrap_instance_event
     @wrap_instance_fault
+    @hooks.add_hook('terminate_instance')
     def terminate_instance(self, context, instance, bdms, reservations):
         """Terminate an instance on this host."""
         # NOTE (ndipanov): If we get non-object BDMs, just get them from the

